[package]
name = "quantum-vault-wasm-prover"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
quantum-vault-crypto = { path = "../crypto" }
hex = "0.4"
getrandom = { version = "0.2", features = ["js"] }
